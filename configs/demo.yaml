game:
  player_count: 7
  max_days: 8
  # roles auto-derived from player_count via WOLFCHA_ROLE_CONFIGS:
  #   2x Werewolf, Seer, Witch, Hunter, Guard, Villager
agents:
  type: llm
  seed: 7
  # Provider resolution: explicit fields below, then LLM_PROVIDER, then the factory default.
  # Official free GLM example: LLM_PROVIDER=bigmodel, GLM_MODEL=glm-4-flash-250414.
  # provider: doubao
  # model: doubao-seed-2-0-pro-250528
  # role_models:
  #   Werewolf:
  #     model: deepseek-v4-pro[1m]
  #   Seer:
  #     model: deepseek-v4-flash[1m]
  #   Witch:
  #     model: kimi-2.6[1m]
  #   Villager:
  #     model: glm-5.1[1m]
